import IconBtn from './IconBtn';

export default function LanguageBtn({ className = '' }: { className?: string }) {
  // 로직 추가하기
  return (
    <IconBtn onClick={() => {}} className={className} src="/svgs/language.svg" alt="language" />
  );
}
